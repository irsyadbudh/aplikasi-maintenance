<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.signuploginrealtime">
    <!-- Replace with your actual package name -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logogedefix"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logogedefix"
        android:supportsRtl="true"
        android:theme="@style/Theme.SignupLoginRealtime"
        tools:targetApi="31">
        <activity
            android:name=".ListItem.ListActivity"
            android:exported="false" />
        <activity
            android:name="QRPage.RekapAdapter"
            android:exported="false"
            tools:ignore="MissingClass">

            <!-- Tambahkan atribut lain yang sesuai -->
        </activity> <!-- Aktivitas RekapActivity -->
        <activity
            android:name=".QRPage.RekapActivity"
            android:exported="false">

            <!-- Tambahkan atribut lain yang sesuai -->
        </activity> <!-- Aktivitas QRActivity -->
        <activity
            android:name=".QRPage.QRActivity"
            android:exported="false">

            <!-- Tambahkan atribut lain yang sesuai -->
        </activity> <!-- Aktivitas MainActivity -->
        <activity
            android:name=".NavigationBar.NavActivity"
            android:exported="false">

            <!-- Tambahkan atribut lain yang sesuai -->
        </activity> <!-- Aktivitas EditProfileActivity -->
        <activity
            android:name=".EditProfileActivity"
            android:exported="false">

            <!-- Tambahkan atribut lain yang sesuai -->
            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity> <!-- Aktivitas LoginActivity -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">

            <!-- Tambahkan atribut lain yang sesuai -->
            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity> <!-- Aktivitas SplashActivity -->
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Aktivitas SignupActivity -->
        <activity
            android:name=".SignupActivity"
            android:exported="false">

            <!-- Tambahkan atribut lain yang sesuai -->
            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>